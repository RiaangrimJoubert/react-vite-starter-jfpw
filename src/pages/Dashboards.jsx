export default function Dashboard() {
  return (
    <div>
      <h1 style={{ fontSize: 32, fontWeight: 900 }}>Dashboard</h1>
      <p style={{ color: "var(--muted)" }}>
        Debit order overview will live here.
      </p>
    </div>
  );
}

